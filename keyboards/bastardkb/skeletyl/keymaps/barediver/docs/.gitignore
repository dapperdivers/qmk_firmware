# Generated by the keymap-drawer pipeline (pre-commit hook / VS Code task).
# Keep README.md and barediver.png; the rest is regenerable intermediate output.
barediver.json
barediver.keymap.yaml
barediver.svg

# QMK's root .gitignore has a blanket `*.png`; re-include the committed diagram.
!barediver.png
